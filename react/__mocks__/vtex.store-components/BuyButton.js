import React, { Component } from 'react'

/**
 * BuyButton Mocked Component.
 */
const BuyButton = ({ children, className }) => {
  return <div className={`buy-button-mock ${className}`}>{children}</div>
}

export default BuyButton
