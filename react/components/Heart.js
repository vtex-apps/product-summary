import React from 'react'
import PropTypes from 'prop-types'
import classNames from 'classnames'
import Image from './Image'

const Heart = ({ src }) => {
  return <div className="" style={{ backgroundColor: `red` }}>:(</div>
}

export default Heart
